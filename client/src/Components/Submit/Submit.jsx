import React from 'react';

const Submit = () => {
    return (
        <div>
            <button type="submit">
                Submit
            </button>
        </div>
    );
};

export default Submit;